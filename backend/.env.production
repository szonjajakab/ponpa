# Production Environment Variables for Cloud Run
# Copy these to Cloud Run environment variables

# Firebase Configuration
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_SERVICE_ACCOUNT_PATH=/app/service-account.json
FIREBASE_STORAGE_BUCKET=your-firebase-project.appspot.com

# Google AI Configuration
GOOGLE_AI_API_KEY=your-google-ai-api-key
GOOGLE_AI_MODEL=gemini-2.5-flash-image-preview

# FastAPI Configuration
ENVIRONMENT=production
DEBUG=false
CORS_ORIGINS=*

# Optional: Custom domain
# ALLOWED_HOSTS=your-domain.com

# Note: Don't commit actual secrets to git!
# Set these in Cloud Run console or via gcloud commands